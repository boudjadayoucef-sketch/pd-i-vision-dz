/**
 * ISO Professional Workspace Interface
 * Exposes the structured ISO professional drafting environment compliant with PD-I_AI_STUDIO_ISO_MASTER
 */

import React from "react";
import IsoWorkspace from "./IsoWorkspace";

export const IsoProfessionalWorkspace: React.FC = () => {
  return <IsoWorkspace />;
};

export default IsoProfessionalWorkspace;
